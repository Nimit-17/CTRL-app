# LLM providers (both free tier). Gemini is primary, Groq is automatic failover.
GEMINI_API_KEY=        # from aistudio.google.com — free tier, do NOT enable billing
GROQ_API_KEY=          # from console.groq.com — free tier

PORT=8787
